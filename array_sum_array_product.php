<?php
/*
array_sum()
the function is used to sum the integer values of array. there will be only one parameter in function.

array_product()
the function is used to multiply the integer values of array, there will be only one parameter in function. 
*/

$values = [1,2,3,4,5,6,7];

//$new = array_sum($values);

$new = array_product($values);
echo $new;